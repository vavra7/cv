# HTML -> PDF (CV)

## Developing
* Open `index.html` with Live Server plugin (in VS Code)

* Run `npm run dev`

## Generating
* Open `index.html` in browser and copy url

* Replace const URL in `index.js` by the current one

* Run `index.js` 

